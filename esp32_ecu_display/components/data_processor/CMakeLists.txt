idf_component_register(SRCS "data_processor.c"
                    INCLUDE_DIRS "."
                    # Add any required dependencies here if needed later
                    # For example, if it uses FreeRTOS queues or semaphores:
                    # REQUIRES freertos
                    )
